## 1. run_ts 唯一后缀

- [x] 1.1 `paths.py make_run_ts`：分钟前缀后追加唯一后缀（秒 + 进程标识，短且文件名安全）
- [x] 1.2 保留 `YYYY-MM-DD-HHMM` 前缀；后缀仅用 `[0-9a-z]`，无路径非法字符
- [x] 1.3 核对 resume / paths 中所有 run_ts 解析点，确认按完整字符串处理（无定长截断）

## 2. 测试

- [x] 2.1 同分钟两次调用 run_ts 不相等（可注入固定 now，断言后缀差异）
- [x] 2.2 前缀格式与排序性
- [x] 2.3 resume 解析新格式 run_ts 回归
- [x] 2.4 `pytest` 全绿
